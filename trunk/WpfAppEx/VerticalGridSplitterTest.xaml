<UserControl x:Class="WpfAppEx.VerticalGridSplitterTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ex="http://schemas.pescuma.org/wpf-ex/2010/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    
	<Grid ex:Grid.Columns="*">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="3" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

		<Label Content="First line" />

		<GridSplitter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="3" Background="LightGray" ResizeDirection="Rows" />

		<Label Content="Second line" />

	</Grid>
</UserControl>